# -ECDSA-Signature-Analyzer-Reuse-Low-S-and-Linear-Dependency-Detection
This Python script analyzes a list of **ECDSA signatures** (`r`, `s`, `z`) to detect potential vulnerabilities related to nonce (`k`) usage. 
